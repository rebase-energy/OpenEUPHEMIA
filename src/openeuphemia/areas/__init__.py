"""Per-area validation cases: one submodule per bidding zone or country.

Each area's inputs and market-building logic live in their own submodule
(``openeuphemia.areas.italy``, and future ones as they're added), all
built on the shared :class:`~openeuphemia.core.PowerMarket` API.
"""
